{
  "name": "vidquest",
  "version": "1.0.0",
  "description": "VidQuest - Plateforme d'investissement",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Static site — no build needed'",
    "sync": "npx cap sync android",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/core": "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}
